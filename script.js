document.querySelectorAll("[data-logo]").forEach(function(img){if(window.UNION_LOGO)img.src=window.UNION_LOGO});
document.querySelectorAll(".filter").forEach(function(button){button.addEventListener("click",function(){document.querySelectorAll(".filter").forEach(b=>b.classList.remove("active"));button.classList.add("active");const cat=button.dataset.filter;document.querySelectorAll(".gallery article").forEach(card=>{card.hidden=cat!=="all"&&card.dataset.cat!==cat})})});
const form=document.querySelector("#quote-form");if(form){form.addEventListener("submit",function(e){e.preventDefault();const d=new FormData(form);const subject=encodeURIComponent("Website Quote Request - "+(d.get("company")||d.get("name")));const body=encodeURIComponent(["Name: "+d.get("name"),"Company: "+d.get("company"),"Phone: "+d.get("phone"),"Email: "+d.get("email"),"Service address: "+d.get("address"),"Service: "+d.get("service"),"Project details: "+d.get("details"),"Photo selected: "+((d.get("photo")&&d.get("photo").name)||"None - attach photos to this email")].join("\n"));const service=String(d.get("service")||"");const designRequest=service==="Permitting"||service==="New sign design / fabrication coordination";const recipient=designRequest?"designs@unionsignco.com":"sales@unionsignco.com";window.location.href="mailto:"+recipient+"?subject="+subject+"&body="+body;document.querySelector("#form-status").textContent="Your email program is opening. Attach any project photos before sending."})}
